import { SearchTitleFilterPipe } from './search-title-filter.pipe';

describe('SearchTitleFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchTitleFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
